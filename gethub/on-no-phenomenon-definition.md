# Phenomenon Definition — On.no Preservation

## Observed phenomenon

A literal notation is supplied:

```
On.no
```

The notation is preserved without silently expanding the period into an undefined operator or deriving an unstated transition.

## Definition

**Bounded notation preservation** — a formal handling rule in which an input expression is retained within its explicitly defined syntax and semantics, and no additional meaning is inferred without a separately defined rule.

In this project record:

```
INPUT:  On.no
RULE:   . = separator
OUTPUT: On.no
```

Therefore:

```
On.no ⇏ ON → NO
On.no ⇏ ON = NO
On.no ⇏ ON causes NO
On.no ⇏ NO = 0
```

## Phenomenon boundary

The observable event here is preservation of notation under the project's stated rules.

```
INPUT → BOUND → LIMIT → CONTROL → PRESERVE → RECORD
```

This is a **project-defined formal behavior**. The record does not establish a newly discovered natural, physical, or universal scientific phenomenon.

## Compact definition

```
On.no → Preserve.
```

Meaning:

> Preserve the supplied notation without unstated expansion.
