# Net / Bet / Result / 1 / 0

## Preserved source

Commit. Net. Bet. Result. 1. 0.

## Definitions

- Net — the remainder after all explicitly included components are combined and applicable offsets are applied.
- Bet — a proposition or wager concerning an uncertain outcome.
- Result — the observed output or state after the specified event, operation, or test.
- 1 — one unit.
- 0 — zero units.

## Framework chain

NET -> BET -> RESULT -> {1, 0}

For a proposition P, if an explicit binary result mapping is declared:

RESULT(P) = 1 when P satisfies the declared success condition.
RESULT(P) = 0 when P satisfies the declared failure condition.

## Boundaries

1 != TRUE unless an explicit truth-value mapping is defined.
0 != FALSE unless an explicit truth-value mapping is defined.
NET != BET.
BET != RESULT.
RESULT != VERIFICATION unless the verification rule has been executed.
GIT COMMIT != BET SETTLEMENT.
GIT COMMIT != BITCOIN TRANSACTION.

No conclusion may exceed its evidentiary derivation.
