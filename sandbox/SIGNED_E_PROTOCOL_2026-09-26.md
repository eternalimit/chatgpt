# Signed E Protocol

User declaration:

> "For now on whenever I said signed. E. That declares IE.R I’ll send a fact."

Operational interpretation:
- `Signed. E.` is Richard Stein's user-defined declaration marker.
- It declares `IE.R` in this workflow and indicates that Richard will supply a fact for evaluation.
- A supplied statement remains user-supplied evidence until independently validated under TCGE.
- `Signed. E.` does not by itself create independent Echo, convert a claim into verified knowledge, or constitute a cryptographic Git signature.

Signed: E

TCGE: K = R AND I AND E.
