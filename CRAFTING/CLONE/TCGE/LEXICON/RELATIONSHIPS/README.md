# RELATIONSHIPS

Records defined relationships among lexicon entries.

Examples:

```text
DEFINE -> SPEC
STATE --TRANSFORMATION--> STATE
SOURCE -> PROVENANCE
OBSERVE -> MEASURE -> COMPARE
```

RELATIONSHIP != CAUSATION unless causation is independently established.
